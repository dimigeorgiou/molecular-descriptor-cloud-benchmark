# RESUME3 — final MISSING/PARTIAL fill
name: rep3_d40k_pow2_spot_low_resume3

mode: compute_only
smiles_complexity: low
descriptor_method: default

dataset_sizes:
  - 40000

node_configs:
  - 16

vcpus_per_node: 4
gb_per_node: 8
use_spot: true
n_replicas: 1

dataset_dir: datasets/samples
