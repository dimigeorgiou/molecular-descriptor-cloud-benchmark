# Warm-keeper rerun: same as coldstart decay pilot but new experiment_id
# after CE minScaleDownDelayMinutes=20 on chemo-ec2-worker-ondemand.
#
# Run:
#   ./scripts/run_coldstart_warmkeeper_pilot.sh

name: pilot_coldstart_warmkeeper_ondemand_rep10

mode: compute_only
smiles_complexity: low
descriptor_method: default

grid_pairs:
  - [5000, 25]

vcpus_per_node: 4
gb_per_node: 8
use_spot: false
n_replicas: 10

dataset_dir: datasets/samples
