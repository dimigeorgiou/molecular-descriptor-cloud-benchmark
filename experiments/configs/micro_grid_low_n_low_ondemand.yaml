# Micro-grid low-N — low complexity, On-Demand.
name: micro_grid_low_n_low_ondemand

mode: compute_only
smiles_complexity: low
descriptor_method: default

dataset_sizes:
  - 5000
  - 10000

node_configs:
  - 1
  - 5
  - 10
  - 15
  - 25

vcpus_per_node: 4
gb_per_node: 8
use_spot: false
n_replicas: 5

dataset_dir: datasets/samples
