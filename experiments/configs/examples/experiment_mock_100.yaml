# Mock/smoke config: 100 rows, 1 node — for tests and quick validation.
name: experiment_mock_100

mode: compute_only

smiles_complexity: medium
descriptor_method: default

dataset_sizes:
  - 100

node_configs:
  - 1

vcpus_per_node: 4
gb_per_node: 8
use_spot: true
n_replicas: 1
