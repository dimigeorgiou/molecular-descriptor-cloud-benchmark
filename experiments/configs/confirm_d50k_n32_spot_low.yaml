# 5-rep confirmation: D=50k Spot low N=32 (scheduling spike outlier).
name: confirm_d50k_n32_spot_low

mode: compute_only
smiles_complexity: low
descriptor_method: default

dataset_sizes:
  - 50000

node_configs:
  - 32

vcpus_per_node: 4
gb_per_node: 8
use_spot: true
n_replicas: 5

dataset_dir: datasets/samples
